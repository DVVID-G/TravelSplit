# Application Configuration
PORT=3000
NODE_ENV=development
API_PREFIX=api
# CORS Configuration (comma-separated list of allowed origins)
# For development, defaults to: http://localhost:5173,http://localhost:3000,http://localhost:5174
# For production, specify your frontend domain(s): https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5174

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=travelsplit
DB_SYNCHRONIZE=true
DB_LOGGING=false

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRES_IN=3600

